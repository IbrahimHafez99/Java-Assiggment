package util;

import java.util.List;

public class LineWrapper {
    public static List<String> wrapWords(String text, int maxLength) {
        // Logic to break text into words and wrap at maxLength
        // Used by left, right, center (but NOT justify)
    }
}
